<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Background image -->

    <!-- Welcome Text (with styled "FishSecure!") -->

    <ImageView
        android:id="@+id/bgImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:alpha="0.70"
        android:scaleType="centerCrop"
        android:src="@drawable/splashback" />

    <!-- Description Text -->

    <!-- Get Started Button -->

    <TextView
        android:id="@+id/tvWelcome"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="140dp"
        android:layout_centerHorizontal="true"
        android:contentDescription="Welcome message"
        android:ellipsize="none"
        android:fontFamily="sans-serif"
        android:gravity="center"
        android:importantForAccessibility="yes"
        android:lineSpacingExtra="2dp"
        android:scrollHorizontally="false"
        android:singleLine="false"
        android:text='Welcome to FishSecure!'
        android:textColor="@android:color/white"
        android:textSize="31sp" />

    <TextView
        android:id="@+id/tvDescription"
        android:layout_width="300dp"
        android:layout_centerHorizontal="true"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvWelcome"
        android:layout_marginTop="9dp"
        android:fontFamily="sans-serif-light"
        android:gravity="center"
        android:lineSpacingExtra="3dp"
        android:text="Keep your aquarium safe, healthy, and thriving with real-time water monitoring at your fingertips."
        android:textColor="@android:color/white"
        android:textSize="15sp" />

    <Button
        android:id="@+id/btnGetStarted"
        android:layout_width="337dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="250dp"
        android:background="@drawable/orange_rounded_button"
        android:elevation="4dp"
        android:fontFamily="sans-serif-medium"
        android:text="GET STARTED"
        android:textAllCaps="true"
        android:textColor="@android:color/white" />
</RelativeLayout>
