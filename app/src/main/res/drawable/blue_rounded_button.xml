<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#1800AD"/>   <!-- Button background color -->
    <corners android:radius="30dp"/>   <!-- Adjust radius for rounded edges -->
    <stroke
        android:width="2dp"
        android:color="@android:color/white"/> <!-- Border color -->
</shape>
